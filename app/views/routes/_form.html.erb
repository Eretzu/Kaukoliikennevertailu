<%= form_with(model: route, local: true) do |form| %>
  <% if route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :city_1_id %>
    <%= form.number_field :city_1_id, id: :route_city_1_id %>
  </div>

  <div class="field">
    <%= form.label :city_2_id %>
    <%= form.number_field :city_2_id, id: :route_city_2_id %>
  </div>

  <div class="field">
    <%= form.label :length %>
    <%= form.number_field :length, id: :route_length %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
